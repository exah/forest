export const id = 'provider'
export const title = 'Provider'

# Provider

```js
import { Theme, Provider } from 'piny-forest'
```

```jsx live
<Provider theme={Theme}>
  <Box bg="primary" s={64} />
</Provider>
```
