export const id = 'zstack'
export const title = 'ZStack'

# ZStack

```js
import { ZStack } from 'piny-forest'
```

Based on [Flex](#flex). Items positioned one after another on z-axis.
[Idea from Swift UI.](https://developer.apple.com/documentation/swiftui/zstack)

🧪 Experimental, can be removed later if there is no enough use cases.

```jsx live
<ZStack
  fg="white"
  bg="lime"
  pss={{
    height: 200,
    alignItems: 'center',
    justifyContent: 'center',
  }}
>
  <Box bg="blue" s={100} />
  <Box bg="red" s={50} />
  <Box bg="yellow" s={75} pss={{ left: 's.16', bottom: 's.16' }} />
</ZStack>
```
