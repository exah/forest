export const title = 'Button'

# Button

```js
import { Button } from 'piny-forest'
```

```jsx live
<Provider
  theme={{
    buttons: {
      primary: {
        backgroundColor: 'primary',
        color: 'white',
        paddingX: 's.8',
        paddingY: 's.4',
        borderRadius: 4,
      },
    },
  }}
>
  <Button variant="primary" onClick={() => alert('Hello')}>
    Say hello
  </Button>
</Provider>
```

```jsx live
<Button type="submit" variant="black">
  Next
</Button>
```

```jsx live
<Button variant="round" aria-label="Add link">
  <PlusIcon />
</Button>
```
