import styled from '@emotion/styled'
import { Base } from '../base'

export const Reset = styled(Base)({
  margin: 0,
  padding: 0,
  border: 0,
  minWidth: 0,
  borderRadius: 0,
  background: 'none',
  font: 'inherit',
  color: 'inherit',
  textAlign: 'inherit',
  textDecoration: 'none',
})
