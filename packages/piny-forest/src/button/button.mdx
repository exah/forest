# Button

```jsx live
<Button
  variant="primary"
  theme={{
    colors: {
      primary: 'royalblue',
    },
    buttonStyles: {
      primary: {
        backgroundColor: 'primary',
        color: 'white',
        paddingX: '0.5rem',
        paddingY: '0.25rem',
        borderRadius: '0.25rem',
      },
    },
  }}
  onClick={() => alert('Hello')}
>
  Say hello
</Button>
```
